#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd -P)"

for test in "$ROOT"/tests/*_test.sh; do
  printf '\n==> %s\n' "${test#$ROOT/}"
  bash "$test"
done

printf '\nAll test scripts passed.\n'
